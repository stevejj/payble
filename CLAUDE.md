# 지갑 없는 날 (Walletless) — 작업 맥락

이 파일은 새 세션이 바로 이어받기 위한 것입니다. 저장소 최상단에 두어 자동으로 읽힙니다.

## 무엇을 만들고 있나

> 지갑이 없어도, 지금 이 자리에서 낼 수 있는 수단을 3초 안에 꺼내주는 아이폰 앱

기획 메모(2026-09-08)의 1차 MVP입니다. **1차 버전이 답해야 하는 질문은 단 하나** —
"이 앱이 카카오페이 켜는 것보다 빠른가." 매출도 사용자 수도 아닙니다.

전제로 깔려 있는 제약: 아이폰은 서드파티 앱이 NFC 접촉 결제를 구현할 수 없고,
카카오페이·네이버페이의 결제 QR도 외부 앱이 만들 수 없습니다.
그래서 만들 수 있는 것은 "모든 결제를 품는 지갑"이 아니라 **"가장 빠른 진입 지점"** 입니다.

## 지켜야 할 원칙 (어기면 이 앱이 아니게 됩니다)

1. **서버도 계정도 없습니다.** 데이터는 기기 밖으로 나가지 않습니다. 결제 관련 정보를
   서버에 두는 순간 보안·법적 책임의 차원이 달라진다는 게 기획 단계의 결정입니다.
2. **브랜드 제휴 정보를 지어내지 않습니다.** "이 매장에서 이 포인트가 적립된다"는
   앱이 알 수 없습니다. 틀리면 사용자가 계산대에서 대신 창피를 당합니다.
   `PlaceCategory`가 장소의 *종류*만 다루는 이유입니다.
3. **URL scheme을 "된다"고 가정하지 않습니다.** 페이 앱 scheme은 공식 문서가 없습니다.
   후보를 여러 개 두고 기기에서 `canOpenURL`로 확인한 것만 씁니다 (설정 → 딥링크 진단).
4. **홈은 목록이 아니라 카드 한 장입니다.** 계산대 앞에서 고르게 만들면 실패입니다.
   월렛처럼 겹쳐 쌓되(`DeckMetrics`), 손이 닿는 것은 맨 앞 한 장입니다.
5. **자동 판단이 사용자가 정한 순서를 조용히 갈아엎지 않습니다.**
   자동 판단은 정확하면 마법 같지만 틀리면 그냥 느린 앱이 됩니다.
6. **최우선 지표는 실행 → 바코드 표시까지의 탭 횟수와 시간입니다.** (`SpeedMetrics`)
   기능을 더할 때도 이 축에서 고릅니다. 개수를 늘리는 방향은 앱을 평범하게 만듭니다.

## 지금까지 만든 것

| | |
|---|---|
| 바코드 저장·전체화면 표시 | `BarcodeStageView` — 밝기 자동 최대, 자동 잠금 해제 |
| 바코드 렌더링 | `EANEncoder`(EAN-13/8·UPC-A 직접 구현) + CoreImage(Code128·QR) |
| 페이 앱 딥링크 | 카카오페이·네이버페이·현대카드·페이코 (`PayAppCatalog`) |
| 위젯 | 홈(small/medium) + 잠금화면 + 제어 센터 컨트롤(iOS 18) |
| 탭 0회 경로 | App Intents — 뒷면 탭·액션 버튼·시리·Spotlight |
| 지갑 없는 날 기록 | `DayLog` — 연속 일수·달력 |
| 카드별 사용처 | `PlaceCategory` — 사용자가 직접 고름 |
| 같은 장소 카드 묶기 | `RankingEngine` — 사용자 순서에서 파생, 맨 앞 카드는 불변. 끌 수 있음 |
| 수동 순서 지정 | `ReorderView` |

의도적으로 뺀 것: 위치 기반 자동 판단(2차), 모바일 신분증, 로그인, 나가기 전 체크.

## 검증 상태 — 여기가 중요합니다

| 항목 | 상태 |
|---|---|
| 컴파일 | **검증됨** — CI가 매 푸시마다 macOS 러너에서 빌드 |
| EAN 인코더 | **검증됨** — 알려진 유효 바코드 5종과 대조 |
| 연속 일수 계산 | **검증됨** — 경계 8종 |
| 페이 앱 URL scheme | **일부 검증** (2026-09-08 실기기) — 아래 참조 |
| T멤버십 바코드 | **검증됨 — 회전형이라 저장 불가** (2026-09-08) |
| 다른 멤버십도 회전형인지 | **미검증** ← 이 앱의 대상 범위를 정하는 질문 |
| 바코드가 매장 스캐너에 읽히는지 | **미검증** ← 가장 중요 |
| 0탭 경로가 카카오페이보다 빠른지 | **미검증** ← 이 앱의 존재 이유 |

### 딥링크 — 2026-09-08 실기기에서 확인한 것

| | 열리나 | 어디로 |
|---|---|---|
| `kakaotalk://kakaopay/home` | 열림 | 화면은 정상. **결제 화면까지는 못 감** |
| `kakaopay://` | 응답함 | **"연결할 수 없는 화면입니다" 오류** — 그래서 후보 순서를 뒤로 뺐다 |
| 네이버페이 | 열림 | **앱 홈**. 홈에 QR이 작게 보이긴 한다 |

**페이 앱 카드가 할 수 있는 것은 "앱을 여는 것"까지다.** 결제 화면 진입은 확인된 방법이 없다.
현대카드·페이코는 미설치라 판정 불가.

### T멤버십은 저장할 수 없다 (2026-09-08 확인)

T멤버십 앱의 바코드는 16자리이고 **20분마다 바뀐다.** 동적(1회용) 바코드다.
스크린샷 재사용을 막으려고 일부러 그렇게 만든 것이고, **그 장치가 이 앱도 똑같이 막는다.**
저장해 둔 번호는 만료된 번호라 계산대에서 거부된다.

**이 앱이 다룰 수 있는 것은 정적인 번호뿐이다.** 보통 실물 카드 뒷면에 인쇄된 번호가 그렇다.
어떤 멤버십이 어느 쪽인지는 **하나씩 기기에서 확인해야 한다.** 추측해서 적지 않는다 —
틀리면 사용자가 계산대에서 대신 창피를 당한다.

확인 방법: 원래 앱을 완전히 종료했다 다시 열어 번호 비교. 같으면 20~30분 뒤 한 번 더.

기획서 7절의 사용자 검증(인터뷰 10명, 2주간 지갑 없이 살기)도 **미완료**입니다.
코드를 더 쌓기 전에 이쪽이 우선입니다.

## 다음에 할 일 (우선순위)

1. ~~실기기에서 **설정 → 딥링크 진단**~~ — 2026-09-08 완료. 위 표 참조
2. ~~등록한 멤버십 바코드가 회전형인지~~ — T멤버십은 회전형으로 확인(위 참조).
   **정적인 번호를 주는 멤버십을 찾는 것**이 다음 일이다. 실물 카드 뒷면부터
3. 편의점 멤버십 등록 후 **실제 계산대에서 인식되는지**
4. **뒷면 탭 vs 카카오페이** 스톱워치 비교 (설정 → 속도에 기록됨)
5. 위가 통과하면 — 위치로 "지금 여기" 판별 (2차).
   순서를 바꾸지 말고 **배지만 붙이는 방식부터**. 맞춰볼 대상은 `PlaceCategory`.

## 명령어

```bash
brew install xcodegen          # 처음 한 번만
make open                      # 프로젝트 생성 후 Xcode로 열기

make free PREFIX=com.본인.walletless   # 무료 Apple ID로 실기기에 올릴 때
make paid                              # 유료 가입 후 App Groups 복구
```

맥북을 처음 세팅한다면 `docs/mac-setup.md`를 따라가면 됩니다.

## 구조

```
Sources/
  Shared/        앱과 위젯이 함께 쓰는 것 (모델 · 저장 · 바코드 인코딩 · 딥링크)
  App/           앱 본체 (화면 · 라우팅 · 속도 계측 · 밝기 제어)
  App/Intents/   시리 · 단축어 · 제어 센터가 부르는 동작
  Widget/        홈 위젯 + 잠금화면 위젯 + 제어 센터 컨트롤
Scripts/
  free-signing.sh  무료 Apple ID용 설정 (make free)
```

앱과 위젯은 **별개 프로세스**라 데이터를 App Group 공유 폴더로 주고받습니다
(`AppGroup.identifier`). 무료 개인 팀은 App Groups를 지원하지 않아 위젯이 카드를
읽지 못하는데, 이때도 `walletless://barcode/top` 링크로 "맨 앞 카드 열기"는 동작합니다.

## 작업 규칙

- 작업 브랜치: `claude/conversation-app-development-3zeyyq` (PR #1)
- 푸시하면 CI가 macOS 러너에서 빌드합니다. **빌드 깨진 채로 두지 마세요.**
- 리눅스 환경에서는 Swift 컴파일을 확인할 수 없습니다. CI가 유일한 검증 수단입니다.
